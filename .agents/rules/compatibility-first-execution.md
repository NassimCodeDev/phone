# Compatibility First Execution

Before implementation:
1. detect the real stack
2. preserve it if it exists
3. initialize the approved default only if no usable frontend exists
4. prefer modifying existing files over creating new ones
5. avoid framework replacement and architecture drift
